services:
  bot:
    build: 
      context: bot
      dockerfile: Dockerfile
    restart: always
    env_file:
      - bot/.env
    environment:
      - DEBUG=False
    networks:
      - bot-network
    

  mongodb:
    image: mongo:latest
    restart: unless-stopped
    env_file:
      - bot/.env
    volumes:
      - ./mongodb/data:/data/db
    networks:
      - bot-network


networks:
  bot-network:
    driver: bridge